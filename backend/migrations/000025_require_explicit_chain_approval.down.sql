BEGIN;

-- Восстановить прежние APPROVED безопасно невозможно: старая схема не хранит,
-- было согласие явным или проставленным автоматически. Откат версии оставляет
-- участников в WAITING и не подделывает пользовательское решение.
SELECT 1;

COMMIT;
